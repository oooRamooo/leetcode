# AGENTS.md

## User Preferences

- LeetCodeの解法を説明するときは、必ず最初にベースのアルゴリズム名を書く。
  - 例: `ベースのアルゴリズム: DFS / Backtracking`
- Javaでコードを書く。
- 解説は日本語で行う。
- 添字、ポインタ、DPの状態は具体例で説明する。
- 難しい式は、なぜその式になるかを具体例から説明する。
- コードを出すときは、必要なら実行できる `main` も付ける。
- ローカル実行の確認が必要な場合は、`javac` と `java` のコマンドも示す。
- ユーザーが明確に「修正して」「作って」「実行できるようにして」など依頼した場合を除き、既存コードを直接編集しない。
